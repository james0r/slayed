export {}

declare global {
  interface Window {
    prodify: {
    }
    variantStrings: {
      addToCart: string,
      unavailable_with_option: string,
      soldout_with_option: string,
      unavailable: string
    }
  }
}